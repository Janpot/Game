var Game = require('../shared/Game.js');

module.exports.load = function (url) {
  
};

var ServerGame = function (cfg) {
  Game.call(this, cfg);
  
  
  
};

ServerGame.prototype = Object.create(Game);